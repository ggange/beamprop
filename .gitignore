/target

# macOS directory metadata
.DS_Store

# Local AI-assistant notes (contributor guidance lives in CONTRIBUTING.md)
/CLAUDE.md

# CLI output artifacts
/out/
*.npy
*.png
*.gif
!docs/**/*.png
# frozen property tables (canonical input data, embedded via include_bytes!:
# a fresh checkout without these does not build). See docs/M4_SPEC.md and
# docs/M6C_SPEC.md.
!data/air_properties.npy
!data/plasma_properties.npy

# Python bytecode (beamprop-py tests)
__pycache__/
